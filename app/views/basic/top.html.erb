<section>
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h1 class="unit__section__title">Contents</h1>
        <ul class="list-group">
          <% @basic_knowledge_categories.each do |category| %>
            <li class="list-group-item">
              <span class="glyphicon glyphicon-book"></span>　
              <%= link_to category.name, "/basiccategory/#{category.id}" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</section>